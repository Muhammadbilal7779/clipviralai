[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "yt-dlp"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npx next start -p ${PORT:-8080}"
