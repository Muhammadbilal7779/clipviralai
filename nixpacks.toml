[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg", "yt-dlp"]

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npx prisma db push --accept-data-loss && npx next start -p ${PORT:-8080}"
